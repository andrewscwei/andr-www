extends /layouts/page
include /includes/nav-button

append vars
  -
    title = `Page Not Found`;
    nav = {
      home: '/',
      logs: '/logs/'
    }

block page
  page-404(id='not-found' name='page')
    +nav-button('down', '/logs/')
    +nav-button('up', '/')
    h1= __('404')
    p= message || __('404-message')
